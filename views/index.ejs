<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-5 w-full">
        <form action="/create" method="post">
          <input
            class="block bg-zinc-700 p-2 m-1 rounded-md w-full text-white"
            type="text"
            name="title"
            id=""
            placeholder="Title"
          />
          <textarea
            class="block resize-none bg-zinc-700 m-1 mt-3 rounded-md w-full p-1 text-white"
            name="details"
           
            id=""
            placeholder="description"
          ></textarea>
          <input
            class="text-white bg-blue-500 p-2 m-2 rounded mt-3"
            type="submit"
            value="create task"
          />
        </form>
      </div>
   <div>
  <p class="text-2xl text-white p-5">Tasks</p>
  <div class="flex flex-wrap gap-1 p-2">
    <% if (files.length > 0) { %>
      <% files.forEach(file => { %>
        <div class="task w-72 px-3 py-4 m-4 rounded-md bg-zinc-700">
          <h1 class="text-white text-2xl"><%= file %></h1>
          <div class="flex justify-between items-center mt-3"> 
              <a href="/file/<%= file %>" class="text-blue-500 inline-block ">Read more</a>
              <a href="/edit/<%= file %>" class="text-zinc-500 inline-block ">Edit</a>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <h1 class="text-zinc-500 text-2xl p-3">no tasks</h1>
    <% } %>
  </div>
      </div>
    </div>
  </body>
</html>
